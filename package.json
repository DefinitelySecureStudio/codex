{
  "name": "@definitely-secure-studio/codex-conformance",
  "version": "0.0.0",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.cjs tests/*.test.mjs",
    "test:comic": "node --test tests/comic-manifest.test.cjs"
  },
  "devDependencies": { "ajv": "8.20.0", "ajv-formats": "3.0.1" }
}
