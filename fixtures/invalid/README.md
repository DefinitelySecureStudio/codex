# Invalid fixtures

Place minimal synthetic counterexamples here, grouped by contract family and
major version. Each fixture must fail for one documented, intentional reason.

- [`prompt-contracts/v1/`](prompt-contracts/v1/) contains single-failure Prompt
  Definition Specification v1.0.0 counterexamples.
