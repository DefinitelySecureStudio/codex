# Valid fixtures

Place minimal synthetic examples here, grouped by contract family and major
version. Each fixture must pass the schema and all normative semantic checks.

- [`prompt-contracts/v1/`](prompt-contracts/v1/) contains valid Prompt
  Definition Specification v1.0.0 documents.
- [`execution-contracts/v1/`](execution-contracts/v1/) contains compatible
  Provider Execution Contract v1.0.0 descriptor, request, and result documents.
