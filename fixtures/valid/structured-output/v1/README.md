# Valid Structured Output v1 fixtures

- `reference-facts.schema.json` is the independently loadable synthetic output schema.
- `validated.structured-output.json` distinguishes retained raw bytes from a canonical parsed and schema-validated value.
- `malformed.structured-output-failure.json` records an explicit syntax failure without retaining the raw body.
- `schema-invalid.structured-output-failure.json` records an explicit schema failure without fabricating normalized data.

All content is synthetic and grants no publication, truth, Canon, or downstream-use approval.
