# Context packages

Contracts for assembling, minimizing, transporting, validating, and expiring
public or approved private runtime context belong here. Real private context
packages must never be committed.
