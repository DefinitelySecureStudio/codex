# YAML schemas

YAML-based schema and validation definitions belong here, grouped by contract
family and major version. YAML examples that are validated by JSON Schema remain
fixtures rather than duplicate schemas.
