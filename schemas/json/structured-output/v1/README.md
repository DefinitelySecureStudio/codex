# Structured Output v1 JSON Schema

[`structured-output.schema.json`](structured-output.schema.json) validates
provider-neutral validated results and explicit processing failures. Semantic
checks still verify raw, normalized, schema artifact, request, and result
identities; schema validation alone is not conformance.
